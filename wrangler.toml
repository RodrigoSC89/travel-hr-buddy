# Cloudflare Pages Configuration
# For static sites (SPA) - NOT Workers

name = "travel-hr-buddy"
compatibility_date = "2024-01-01"

# Assets directory for Cloudflare Pages
[assets]
directory = "./dist"

[build]
command = "npm run build"
