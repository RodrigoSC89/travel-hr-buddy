# Cloudflare Pages Configuration
# Forces npm instead of bun to avoid lockfile conflicts

name = "travel-hr-buddy"
compatibility_date = "2024-01-01"

[build]
command = "npm ci && npm run build"

[build.environment]
NODE_VERSION = "20"
NPM_FLAGS = "--legacy-peer-deps"
