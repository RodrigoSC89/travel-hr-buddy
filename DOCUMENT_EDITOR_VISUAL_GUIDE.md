# Document Editor Visual Guide

## 🎨 Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                     Collaborative Document Editor                │
└─────────────────────────────────────────────────────────────────┘

┌─────────────┐                                      ┌─────────────┐
│   User A    │                                      │   User B    │
│  Browser 1  │                                      │  Browser 2  │
└──────┬──────┘                                      └──────┬──────┘
       │                                                    │
       │ 1. User types                          1. User types │
       │                                                    │
       ▼                                                    ▼
┌─────────────┐                                      ┌─────────────┐
│   TipTap    │                                      │   TipTap    │
│   Editor    │                                      │   Editor    │
└──────┬──────┘                                      └──────┬──────┘
       │                                                    │
       │ 2. Update                              2. Update   │
       │                                                    │
       ▼                                                    ▼
┌─────────────┐         3. Sync via WebRTC          ┌─────────────┐
│     Yjs     │ ◄─────────────────────────────────► │     Yjs     │
│  Document   │      (P2P Real-time Sync)           │  Document   │
│   (CRDT)    │                                      │   (CRDT)    │
└──────┬──────┘                                      └──────┬──────┘
       │                                                    │
       │ 4. Auto-save (debounced 3s)        4. Auto-save   │
       │                                                    │
       ▼                                                    ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Supabase Database                           │
│                      documents table                             │
│                    (Row Level Security)                          │
└─────────────────────────────────────────────────────────────────┘
```

## 🔄 Data Flow

### 1. User Types → Editor Update
```
User Input ──► TipTap Editor ──► onUpdate callback
```

### 2. Editor → Yjs CRDT
```
Editor Content ──► Yjs Document (CRDT) ──► Automatic Conflict Resolution
```

### 3. Yjs → WebRTC Sync
```
Yjs Document ──► WebRTC Provider ──► Signaling Server ──► Other Users
```

### 4. Yjs → Database Save
```
Yjs Document ──► Debounce (3s) ──► Supabase ──► documents table
```

## 📊 Component Structure

```
DocumentEditor
│
├── State Management
│   ├── saving (boolean)
│   ├── lastSaved (Date | null)
│   ├── connectedUsers (number)
│   └── versions (number)
│
├── Refs
│   ├── ydocRef (Y.Doc)
│   └── providerRef (WebrtcProvider)
│
├── Effects
│   ├── Initialize Yjs + WebRTC
│   ├── Load initial content
│   └── Track connected users
│
├── TipTap Editor
│   ├── Extensions
│   │   ├── StarterKit (history disabled)
│   │   ├── Collaboration (with Yjs doc)
│   │   └── CollaborationCursor (with user info)
│   │
│   └── Callbacks
│       └── onUpdate → debounced save
│
└── UI Components
    ├── Card (container)
    ├── Toolbar (formatting buttons)
    ├── EditorContent (TipTap)
    └── Status bar (users, timestamp, save button)
```

## 🎯 Real-time Sync Flow

```
Step 1: User A types "Hello"
┌─────────┐
│ User A  │ Types: "Hello"
└────┬────┘
     │
     ▼
┌─────────┐
│ Yjs Doc │ State: "Hello"
│ (A)     │
└────┬────┘
     │ WebRTC
     │ ────────►
     ▼
┌─────────┐
│ Yjs Doc │ State: "Hello" (synced)
│ (B)     │
└────┬────┘
     │
     ▼
┌─────────┐
│ User B  │ Sees: "Hello"
└─────────┘

Step 2: User B adds " World"
┌─────────┐
│ User B  │ Types: " World"
└────┬────┘
     │
     ▼
┌─────────┐
│ Yjs Doc │ State: "Hello World"
│ (B)     │
└────┬────┘
     │ WebRTC
     │ ◄────────
     ▼
┌─────────┐
│ Yjs Doc │ State: "Hello World" (synced)
│ (A)     │
└────┬────┘
     │
     ▼
┌─────────┐
│ User A  │ Sees: "Hello World"
└─────────┘
```

## 🔐 Security Flow

```
┌─────────────┐
│    User     │
└──────┬──────┘
       │
       │ 1. Login
       ▼
┌─────────────┐
│ Supabase    │
│    Auth     │
└──────┬──────┘
       │
       │ 2. JWT Token
       ▼
┌─────────────┐
│ Document    │
│  Editor     │
└──────┬──────┘
       │
       │ 3. Save Request
       ▼
┌─────────────┐
│ Supabase    │
│  Database   │
└──────┬──────┘
       │
       │ 4. Check RLS Policy
       ▼
┌─────────────┐
│  auth.uid() │
│ IS NOT NULL?│
└──────┬──────┘
       │
       ├── Yes ──► Allow
       └── No  ──► Deny
```

## 🎨 UI Layout

```
┌────────────────────────────────────────────────────────────┐
│  📝 Collaborative Document Editor                          │
│                                    👥 3 users  🕐 14:23:45  │
├────────────────────────────────────────────────────────────┤
│                                                            │
│  ┌─────┬─────┬────┬────┬──────┬──────┐                   │
│  │  B  │  I  │ H1 │ H2 │ List │ Code │  ◄── Toolbar     │
│  └─────┴─────┴────┴────┴──────┴──────┘                   │
│                                                            │
│  ┌────────────────────────────────────────────────────┐   │
│  │                                                    │   │
│  │  Editor Content Area                              │   │
│  │                                                    │   │
│  │  Users can type here...                           │   │
│  │                                                    │   │
│  │  • Bold, italic, headings                         │   │
│  │  • Lists, code blocks                             │   │
│  │  • Real-time sync                                 │   │
│  │                                                    │   │
│  │  ┌──────────────┐                                 │   │
│  │  │ User A's     │  ◄── Colored cursor indicators  │   │
│  │  │ cursor       │                                 │   │
│  │  └──────────────┘                                 │   │
│  │                                                    │   │
│  └────────────────────────────────────────────────────┘   │
│                                                            │
│  ┌──────────┐      💾 Auto-save: Every 3 seconds          │
│  │ Save Now │      🔄 Versions saved: 5                   │
│  └──────────┘                                             │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

## 📈 Performance Timeline

```
Time: 0ms
┌────────────────┐
│ Page Load      │
└────────────────┘
       │
Time: 500ms
┌────────────────┐
│ Editor Ready   │ ◄── User can see editor
└────────────────┘
       │
Time: 600ms
┌────────────────┐
│ First Edit     │ ◄── User starts typing
└────────────────┘
       │
Time: 650ms
┌────────────────┐
│ P2P Sync       │ ◄── Change appears in other browsers
└────────────────┘
       │
Time: 3650ms
┌────────────────┐
│ Auto-save      │ ◄── Saved to database
└────────────────┘
```

## 🔄 Conflict Resolution (CRDT)

```
Scenario: Two users edit the same document simultaneously

User A:                     User B:
"Hello"                     "Hello"
  │                           │
  ├─ Types "!"                ├─ Types " World"
  │                           │
  ▼                           ▼
"Hello!"                    "Hello World"

        WebRTC Sync
              │
              ▼
      Yjs CRDT Merge
              │
              ▼
       "Hello World!"  ◄── Automatically merged!
              │
       ┌──────┴──────┐
       ▼             ▼
   User A        User B
 "Hello World!" "Hello World!"
```

## 🎯 Demo Page Layout

```
┌────────────────────────────────────────────────────────────┐
│  Collaborative Document Editor Demo                        │
│  Experience real-time collaborative editing...             │
├────────────────────────────────────────────────────────────┤
│  Document ID Management                                    │
│  ┌────────────────────────────────┬──────┬──────┐         │
│  │ 550e8400-e29b-41d4-a716-...   │ Copy │  🔄  │         │
│  └────────────────────────────────┴──────┴──────┘         │
│                                                            │
│  📋 How to test collaboration:                            │
│  1. Copy the Document ID above                            │
│  2. Open this page in another tab                         │
│  3. Paste the same ID in the other tab                    │
│  4. Start editing - changes sync in real-time!            │
├────────────────────────────────────────────────────────────┤
│  ✨ Features                                               │
│  ┌──────────────────┬──────────────────┐                 │
│  │ 🔄 Real-time     │ 🚀 Conflict-free │                 │
│  │ Collaboration    │ Merging          │                 │
│  ├──────────────────┼──────────────────┤                 │
│  │ 💾 Auto-save     │ 👥 User Presence │                 │
│  └──────────────────┴──────────────────┘                 │
├────────────────────────────────────────────────────────────┤
│  🔧 Technical Architecture                                 │
│  User Input → TipTap → Yjs → WebRTC → Other Users        │
│                  ↓                                         │
│            Debounce (3s)                                   │
│                  ↓                                         │
│          Supabase Database                                 │
├────────────────────────────────────────────────────────────┤
│  📝 Editor (shown here)                                    │
├────────────────────────────────────────────────────────────┤
│  📊 Performance Metrics                                    │
│  ~500ms    <100ms     50-100ms    100-200ms    ~10MB      │
│  Initial   First Edit  Sync (2)   Sync (10)   Memory     │
│  Load                 users       users                    │
└────────────────────────────────────────────────────────────┘
```

## 📦 Dependencies Diagram

```
DocumentEditor Component
        │
        ├── React
        │   └── Hooks (useEffect, useRef, useState)
        │
        ├── TipTap
        │   ├── @tiptap/react
        │   ├── @tiptap/starter-kit
        │   ├── @tiptap/extension-collaboration
        │   └── @tiptap/extension-collaboration-cursor
        │
        ├── Yjs
        │   ├── yjs (CRDT library)
        │   └── y-webrtc (WebRTC provider)
        │
        ├── Utilities
        │   └── lodash (debounce)
        │
        ├── UI Components
        │   ├── Card, Button, Input
        │   └── Icons (Loader2, Save, Users, Clock)
        │
        ├── Supabase
        │   └── Database client
        │
        └── Auth
            └── AuthContext (user info)
```

## 🎉 Summary

This visual guide shows the complete architecture, data flow, and UI layout of the Collaborative Document Editor. The system uses modern web technologies to provide real-time, conflict-free collaborative editing with automatic persistence.

For more details, see:
- **Usage Guide**: `DOCUMENT_EDITOR_GUIDE.md`
- **Implementation**: `DOCUMENT_EDITOR_IMPLEMENTATION_SUMMARY.md`
- **Quick Start**: `DOCUMENT_EDITOR_QUICKSTART.md`
