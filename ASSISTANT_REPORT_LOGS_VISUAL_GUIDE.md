# Assistant Report Logs - Visual Implementation Guide

## 🎨 Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                       Frontend (React)                       │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  User triggers "Send Report" action                    │ │
│  │  ↓                                                      │ │
│  │  Collect assistant logs from database                  │ │
│  │  ↓                                                      │ │
│  │  Call /functions/v1/send-assistant-report API          │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────┐
│            Supabase Edge Function (Deno)                     │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  1. Authenticate user via Authorization header         │ │
│  │  2. Validate request data (logs array)                 │ │
│  │  3. Prepare email HTML report                          │ │
│  │  4. Log attempt to assistant_report_logs table         │ │
│  │  5. Return success/error response                      │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              ↓
┌─────────────────────────────────────────────────────────────┐
│              Supabase PostgreSQL Database                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              assistant_report_logs table                │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │  • id (UUID)                                      │  │ │
│  │  │  • user_id (UUID) → auth.users                   │  │ │
│  │  │  • user_email (TEXT)                             │  │ │
│  │  │  • status (TEXT) - 'success' or 'error'          │  │ │
│  │  │  • sent_at (TIMESTAMPTZ)                         │  │ │
│  │  │  • message (TEXT)                                │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## 🔄 Request Flow Diagram

```
┌──────────┐
│  Client  │
└────┬─────┘
     │
     │ POST /functions/v1/send-assistant-report
     │ Authorization: Bearer <token>
     │ Body: { logs: [...], toEmail: "...", subject: "..." }
     │
     ↓
┌────────────────────────────────────────────────────────┐
│  Edge Function: send-assistant-report                   │
│                                                          │
│  ┌────────────────────────────────────────────────┐    │
│  │  Step 1: CORS Check                            │    │
│  │  ✓ Handle OPTIONS request                      │    │
│  └────────────────────────────────────────────────┘    │
│                       │                                 │
│                       ↓                                 │
│  ┌────────────────────────────────────────────────┐    │
│  │  Step 2: Authentication                        │    │
│  │  ✓ Create Supabase client                      │    │
│  │  ✓ Extract Authorization header                │    │
│  │  ✓ Verify user via auth.getUser()              │    │
│  │  ✗ Return 401 if not authenticated             │    │
│  └────────────────────────────────────────────────┘    │
│                       │                                 │
│                       ↓                                 │
│  ┌────────────────────────────────────────────────┐    │
│  │  Step 3: Validation                            │    │
│  │  ✓ Parse request body                          │    │
│  │  ✓ Check logs array exists and not empty       │    │
│  │  ✗ Log error + Return 400 if invalid           │────┐
│  └────────────────────────────────────────────────┘    │
│                       │                                 │
│                       ↓                                 │
│  ┌────────────────────────────────────────────────┐    │
│  │  Step 4: Prepare Report                        │    │
│  │  ✓ Get email config from env vars              │    │
│  │  ✓ Build HTML table from logs                  │    │
│  │  ✓ Create email message object                 │    │
│  └────────────────────────────────────────────────┘    │
│                       │                                 │
│                       ↓                                 │
│  ┌────────────────────────────────────────────────┐    │
│  │  Step 5: Log Success                           │    │
│  │  ✓ Insert to assistant_report_logs:            │◄───┘
│  │    - user_id                                    │
│  │    - user_email                                 │
│  │    - status: "success" or "error"               │
│  │    - message                                    │
│  │  ✓ Return 200 with success response            │
│  └────────────────────────────────────────────────┘
│                       │
│  [On Error]          ↓
│  ┌────────────────────────────────────────────────┐
│  │  Step 6: Error Handling                        │
│  │  ✓ Catch exception                              │
│  │  ✓ Try to log error to database                │
│  │  ✓ Return 500 with error details               │
│  └────────────────────────────────────────────────┘
│                                                          │
└──────────────────────────────────────────────────────────┘
```

## 📊 Database Schema Visualization

```sql
┌───────────────────────────────────────────────────────────┐
│              assistant_report_logs                         │
├─────────────────────┬─────────────────┬───────────────────┤
│ Column              │ Type            │ Constraints       │
├─────────────────────┼─────────────────┼───────────────────┤
│ id                  │ UUID            │ PRIMARY KEY       │
│                     │                 │ DEFAULT gen_...() │
├─────────────────────┼─────────────────┼───────────────────┤
│ user_id             │ UUID            │ NOT NULL          │
│                     │                 │ FK → auth.users   │
│                     │                 │ ON DELETE CASCADE │
├─────────────────────┼─────────────────┼───────────────────┤
│ user_email          │ TEXT            │ NOT NULL          │
├─────────────────────┼─────────────────┼───────────────────┤
│ status              │ TEXT            │ NOT NULL          │
│                     │                 │ CHECK IN          │
│                     │                 │ ('success','error')│
├─────────────────────┼─────────────────┼───────────────────┤
│ sent_at             │ TIMESTAMPTZ     │ DEFAULT NOW()     │
├─────────────────────┼─────────────────┼───────────────────┤
│ message             │ TEXT            │ NOT NULL          │
└─────────────────────┴─────────────────┴───────────────────┘

Indexes:
  ✓ idx_assistant_report_logs_user_id (user_id)
  ✓ idx_assistant_report_logs_sent_at (sent_at DESC)
  ✓ idx_assistant_report_logs_status (status)

RLS Policies:
  ✓ Users can insert their own logs
  ✓ Users can view their own logs  
  ✓ Admin users can view all logs
```

## 🔐 Security Model

```
┌──────────────────────────────────────────────────────────┐
│                Row Level Security (RLS)                   │
├──────────────────────────────────────────────────────────┤
│                                                            │
│  Policy 1: "Users can insert their own report logs"      │
│  ┌──────────────────────────────────────────────────┐    │
│  │  FOR INSERT                                       │    │
│  │  WITH CHECK (auth.uid() = user_id)               │    │
│  └──────────────────────────────────────────────────┘    │
│                                                            │
│  Policy 2: "Users can view their own report logs"        │
│  ┌──────────────────────────────────────────────────┐    │
│  │  FOR SELECT                                       │    │
│  │  USING (auth.uid() = user_id)                    │    │
│  └──────────────────────────────────────────────────┘    │
│                                                            │
│  Policy 3: "Admin users can view all report logs"        │
│  ┌──────────────────────────────────────────────────┐    │
│  │  FOR SELECT                                       │    │
│  │  USING (                                          │    │
│  │    EXISTS (                                       │    │
│  │      SELECT 1 FROM profiles                       │    │
│  │      WHERE profiles.id = auth.uid()              │    │
│  │      AND profiles.role = 'admin'                 │    │
│  │    )                                              │    │
│  │  )                                                │    │
│  └──────────────────────────────────────────────────┘    │
│                                                            │
└──────────────────────────────────────────────────────────┘
```

## 📝 Logging Scenarios

### Scenario 1: Successful Report Send

```
User Action: Click "Send Report" button
     ↓
Request: POST /functions/v1/send-assistant-report
     ↓
Validation: ✓ User authenticated
     ↓           ✓ Logs array has data
     ↓           ✓ Email prepared
     ↓
Database Insert:
┌────────────────────────────────────────────┐
│ user_id: "123e4567-e89b..."                │
│ user_email: "john@example.com"             │
│ status: "success"                          │
│ message: "Enviado com sucesso"             │
│ sent_at: "2025-10-12 19:00:00+00"          │
└────────────────────────────────────────────┘
     ↓
Response: 200 OK
{
  "success": true,
  "message": "Relatório preparado...",
  "logsCount": 15
}
```

### Scenario 2: Empty Logs Array

```
User Action: Click "Send Report" with no data
     ↓
Request: POST /functions/v1/send-assistant-report
     ↓
Validation: ✓ User authenticated
     ↓           ✗ Logs array is empty
     ↓
Database Insert:
┌────────────────────────────────────────────┐
│ user_id: "123e4567-e89b..."                │
│ user_email: "john@example.com"             │
│ status: "error"                            │
│ message: "Nenhum dado para enviar."        │
│ sent_at: "2025-10-12 19:00:00+00"          │
└────────────────────────────────────────────┘
     ↓
Response: 400 Bad Request
{
  "error": "Nenhum dado para enviar."
}
```

### Scenario 3: Authentication Error

```
User Action: Call API without token
     ↓
Request: POST /functions/v1/send-assistant-report
     ↓
Validation: ✗ No Authorization header
     ↓
Response: 401 Unauthorized
{
  "error": "Não autenticado"
}

(No database log created - user unknown)
```

## 📈 Monitoring Dashboard Example

```sql
-- Daily Report Activity
SELECT 
  DATE(sent_at) as report_date,
  COUNT(*) as total_reports,
  COUNT(*) FILTER (WHERE status = 'success') as successful,
  COUNT(*) FILTER (WHERE status = 'error') as failed,
  ROUND(
    100.0 * COUNT(*) FILTER (WHERE status = 'success') / COUNT(*),
    2
  ) as success_rate_percent
FROM assistant_report_logs
WHERE sent_at >= CURRENT_DATE - INTERVAL '30 days'
GROUP BY DATE(sent_at)
ORDER BY report_date DESC;
```

**Example Output:**
```
┌──────────────┬───────────────┬────────────┬────────┬─────────────────────┐
│ report_date  │ total_reports │ successful │ failed │ success_rate_percent│
├──────────────┼───────────────┼────────────┼────────┼─────────────────────┤
│ 2025-10-12   │ 15            │ 14         │ 1      │ 93.33               │
│ 2025-10-11   │ 23            │ 22         │ 1      │ 95.65               │
│ 2025-10-10   │ 18            │ 18         │ 0      │ 100.00              │
└──────────────┴───────────────┴────────────┴────────┴─────────────────────┘
```

## 🎯 Key Features Visualization

```
┌─────────────────────────────────────────────────────────┐
│                  Implementation Benefits                 │
├─────────────────────────────────────────────────────────┤
│                                                           │
│  📊 Observability                                        │
│  ├─ Track all report sending attempts                   │
│  ├─ Monitor success/failure rates                       │
│  └─ Identify patterns and trends                        │
│                                                           │
│  🔍 Debugging                                            │
│  ├─ Detailed error messages                             │
│  ├─ Timestamp of each attempt                           │
│  └─ User context for troubleshooting                    │
│                                                           │
│  🔐 Security & Compliance                                │
│  ├─ RLS policies enforce data privacy                   │
│  ├─ Complete audit trail                                │
│  └─ User action tracking                                │
│                                                           │
│  ⚡ Performance                                           │
│  ├─ Indexed columns for fast queries                    │
│  ├─ Efficient data retrieval                            │
│  └─ Minimal overhead on main flow                       │
│                                                           │
└─────────────────────────────────────────────────────────┘
```

## 🔧 Integration Example

```typescript
// Frontend component example
import { useState } from "react";
import { supabase } from "@/lib/supabase";
import { Button } from "@/components/ui/button";
import { toast } from "@/hooks/use-toast";

export function SendReportButton({ logs }: { logs: AssistantLog[] }) {
  const [loading, setLoading] = useState(false);

  const handleSendReport = async () => {
    setLoading(true);
    
    try {
      const { data: { session } } = await supabase.auth.getSession();
      
      if (!session) {
        toast({
          title: "Erro",
          description: "Você precisa estar autenticado.",
          variant: "destructive",
        });
        return;
      }

      const response = await fetch(
        `${import.meta.env.VITE_SUPABASE_URL}/functions/v1/send-assistant-report`,
        {
          method: "POST",
          headers: {
            Authorization: `Bearer ${session.access_token}`,
            "Content-Type": "application/json",
          },
          body: JSON.stringify({ logs }),
        }
      );

      if (!response.ok) {
        throw new Error("Failed to send report");
      }

      const result = await response.json();
      
      toast({
        title: "✅ Sucesso",
        description: `Relatório enviado com ${result.logsCount} interações.`,
      });
      
    } catch (error) {
      toast({
        title: "❌ Erro",
        description: "Falha ao enviar relatório.",
        variant: "destructive",
      });
    } finally {
      setLoading(false);
    }
  };

  return (
    <Button 
      onClick={handleSendReport} 
      disabled={loading || logs.length === 0}
    >
      {loading ? "Enviando..." : "📧 Enviar Relatório"}
    </Button>
  );
}
```

## ✅ Verification Checklist

```
Database Migration
  ✓ Table created with correct schema
  ✓ Indexes added for performance
  ✓ RLS policies configured
  ✓ Foreign key constraint to auth.users
  ✓ Check constraints on status field

Edge Function
  ✓ Supabase client initialized
  ✓ User authentication implemented
  ✓ Request validation added
  ✓ Success logging implemented
  ✓ Error logging implemented
  ✓ Graceful error handling

Security
  ✓ RLS enabled on table
  ✓ User can only insert/view own logs
  ✓ Admin can view all logs
  ✓ Authorization required for API

Documentation
  ✓ Implementation guide created
  ✓ Quick reference guide created
  ✓ Visual guide created
  ✓ API usage examples provided
```
