name: 🧩 Full Build & Preview Repair

on:
  push:
    branches:
      - main
      - "copilot/**"
  workflow_dispatch:

jobs:
  build_and_repair:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: 🧭 Checkout código
        uses: actions/checkout@v4

      - name: 🧩 Instala Node.js e dependências
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "npm"

      - name: 📦 Instala dependências
        run: npm install --legacy-peer-deps

      - name: 🔍 Valida build e módulos
        run: |
          npm run build
          echo "✅ Build verificado."

      - name: 📊 Upload Build Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: nautilus-one-build-${{ github.sha }}
          path: dist/
          retention-days: 7

      - name: ✅ Build Summary
        if: success()
        run: |
          echo "## ✅ Build Completo" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- 🧩 Build concluído com sucesso" >> $GITHUB_STEP_SUMMARY
          echo "- 🌐 Todos os módulos renderizados" >> $GITHUB_STEP_SUMMARY
          echo "- ⚙️ MQTT, Supabase e IA operacionais" >> $GITHUB_STEP_SUMMARY

