name: CI - Nautilus One

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout código
        uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
          cache: 'npm'

      - name: Instalar dependências
        run: npm install

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Testes automatizados
        run: npm run test

      - name: "(Opcional) Deploy para Vercel (automático)"
        if: success()
        run: echo "🔁 Deploy pode ser ativado aqui com token Vercel"
