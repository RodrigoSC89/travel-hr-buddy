/**
 * REDIRECT: predictive-analytics → nautilus-ai-hub
 * Este módulo foi fundido no Nautilus AI Hub
 */
import { Navigate } from "react-router-dom";
export default function PredictiveAnalyticsRedirect() {
  return <Navigate to="/nautilus-ai-hub" replace />;
}
