/**
 * REDIRECT: resource-availability → nautilus-voyage
 * Este módulo foi fundido no Nautilus Voyage
 */
import { Navigate } from "react-router-dom";
export default function ResourceAvailabilityRedirect() {
  return <Navigate to="/nautilus-voyage" replace />;
}
