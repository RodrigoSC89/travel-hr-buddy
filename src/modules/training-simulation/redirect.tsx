/**
 * REDIRECT: training-simulation → nautilus-academy
 * Este módulo foi fundido no Nautilus Academy
 */
import { Navigate } from "react-router-dom";
export default function TrainingSimulationRedirect() {
  return <Navigate to="/nautilus-academy" replace />;
}
