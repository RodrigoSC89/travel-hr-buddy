/**
 * DEPRECATED: This module has been merged into Reports Command Center
 * Redirects to /reports-command
 */
import { Navigate } from "react-router-dom";

const Reports = () => {
  return <Navigate to="/reports-command" replace />;
};

export default Reports;
