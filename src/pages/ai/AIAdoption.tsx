/**
 * REDIRECT: AI Adoption → AI Command Center
 * PATCH UNIFY-11.1 - Este módulo foi fundido no AI Command Center
 */
import { Navigate } from "react-router-dom";

export default function AIAdoption() {
  return <Navigate to="/ai-command" replace />;
}
