# BridgeLink Integration Core - Visual Summary

## 🎨 UI Components

### Main Page Layout

```
┌─────────────────────────────────────────────────────────────┐
│  🧭 BridgeLink Integration Core                            │
│  Monitoramento em tempo real de sistemas DP, ASOG, FMEA    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  📡 Status de Comunicação                                   │
│  ┌──────────┬──────────┬──────────┐                        │
│  │ Online   │ Latência │ Última   │                        │
│  │ Conectado│ 45.2 ms  │ Sync     │                        │
│  │          │          │ 14:30:15 │                        │
│  └──────────┴──────────┴──────────┘                        │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  🔄 Sincronização em Tempo Real                            │
│  ┌─────────┬─────────┬─────────┐                          │
│  │ ☁️ MQTT │ 🗄️ Supa │ 🔄 Stat │                          │
│  │ Ativo   │ Conecta │ Última  │                          │
│  │         │ do      │ atual:  │                          │
│  │         │         │ 14:30:16│                          │
│  └─────────┴─────────┴─────────┘                          │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│  ⚡ Painel de Diagnóstico                                   │
│  ┌───────────────────────────┐                             │
│  │ 🔄 Forçar Sincronização   │                             │
│  └───────────────────────────┘                             │
│  ─────────────────────────────────────────                 │
│  📋 Event Log:                                             │
│  • Sincronização manual iniciada às 14:30:16              │
│  • Sincronização manual iniciada às 14:25:10              │
└─────────────────────────────────────────────────────────────┘
```

## 🏗️ Component Hierarchy

```
BridgeLink.tsx
├── Suspense (with Loading fallback)
│   └── main (container)
│       ├── h1 (title)
│       ├── BridgeLinkStatus
│       │   └── Card
│       │       ├── CardHeader (with Wifi icon)
│       │       └── CardContent
│       │           └── Grid (3 columns)
│       │               ├── Metric (Online)
│       │               ├── Metric (Latency)
│       │               └── Metric (Last Sync)
│       ├── BridgeLinkSync
│       │   └── Card
│       │       ├── CardHeader (with RefreshCw icon)
│       │       └── CardContent
│       │           └── Flex (3 items)
│       │               ├── StatusItem (MQTT)
│       │               ├── StatusItem (Supabase)
│       │               └── StatusItem (Status)
│       └── BridgeLinkDashboard
│           └── Card
│               ├── CardHeader (with Activity icon)
│               └── CardContent
│                   ├── Button (Force Sync)
│                   └── Event Log
```

## 🔄 Data Flow Diagram

```
┌──────────────┐
│   Supabase   │
│   Database   │
└──────┬───────┘
       │ Real-time
       │ Channel
       ▼
┌──────────────┐      ┌──────────────┐
│ BridgeLink   │─────▶│     MQTT     │
│    Sync      │      │    Broker    │
└──────────────┘      └──────┬───────┘
                             │
       ┌─────────────────────┼─────────────────────┐
       │                     │                     │
       ▼                     ▼                     ▼
┌──────────┐          ┌──────────┐          ┌──────────┐
│    DP    │          │ Forecast │          │ Control  │
│  Module  │          │  Module  │          │   Hub    │
└──────────┘          └──────────┘          └──────────┘
```

## 🎯 State Management

```
BridgeLinkStatus
├── status: { online, latency, lastSync }
└── useEffect: subscribeBridgeStatus

BridgeLinkSync
├── syncStatus: string
└── useEffect: Supabase channel subscription

BridgeLinkDashboard
├── log: string[]
└── triggerSync: function
```

## 📊 MQTT Topic Flow

```
Publishing:
┌──────────────────────────────────────────┐
│ Component Action                         │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│ publishEvent(topic, payload)             │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│ MQTT Broker                              │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│ Subscribed Clients Receive Update        │
└──────────────────────────────────────────┘

Subscribing:
┌──────────────────────────────────────────┐
│ subscribeBridgeStatus(callback)          │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│ MQTT Client subscribes to topic          │
└──────────┬───────────────────────────────┘
           │
           ▼
┌──────────────────────────────────────────┐
│ callback(data) when message arrives      │
└──────────────────────────────────────────┘
```

## 🎨 Color Scheme

| Element | Color | Usage |
|---------|-------|-------|
| Icons | `text-primary` | Wifi, RefreshCw, Activity icons |
| Background | `bg-background` | Page background |
| Text | `text-foreground` | Main text |
| Button | `bg-primary` | Force Sync button |
| Button Text | `text-primary-foreground` | Button text |
| Muted Text | `text-gray-400` | Labels, secondary info |

## 🔧 Component Loading States

```
Initial Load:
┌─────────────────────────────────────┐
│  Loading Component...               │
│  ⏳ Carregando BridgeLink...        │
└─────────────────────────────────────┘

Error State:
┌─────────────────────────────────────┐
│  ⚠️ Falha ao carregar o módulo      │
│  BridgeLink                         │
│  [🔄 Atualizar página]              │
└─────────────────────────────────────┘

Loaded State:
┌─────────────────────────────────────┐
│  🧭 BridgeLink Integration Core     │
│  [Status, Sync, Dashboard cards]    │
└─────────────────────────────────────┘
```

## 📱 Responsive Layout

### Desktop (≥768px)
```
┌────────────────────────────────────┐
│  Title                             │
├────────────────────────────────────┤
│  [Status] [Sync] [Dashboard]       │
│   Full   Width   Cards             │
└────────────────────────────────────┘
```

### Mobile (<768px)
```
┌──────────────┐
│  Title       │
├──────────────┤
│  [Status]    │
├──────────────┤
│  [Sync]      │
├──────────────┤
│  [Dashboard] │
└──────────────┘
```

## 🎭 Interactive Elements

### Force Sync Button
```
Normal State:
┌───────────────────────────┐
│ 🔄 Forçar Sincronização   │
└───────────────────────────┘

Hover State:
┌───────────────────────────┐
│ 🔄 Forçar Sincronização   │
│ (opacity: 90%)            │
└───────────────────────────┘

Clicked:
1. publishEvent() is called
2. Log entry is added
3. Visual feedback in event log
```

## 📈 Metrics Display

```
Metric Component:
┌─────────────┐
│   Label     │  ← text-sm text-gray-400
├─────────────┤
│   Value     │  ← text-{color}-400 font-semibold
└─────────────┘

StatusItem Component:
┌─────────────┐
│    Icon     │  ← text-primary
├─────────────┤
│   Label     │  ← text-gray-400 text-sm
├─────────────┤
│   Value     │  ← text-sm font-semibold
└─────────────┘
```

## 🔍 Accessibility Features

- ✅ `role="heading"` on title
- ✅ `aria-level={1}` on main heading
- ✅ Semantic HTML structure
- ✅ Loading states with proper messages
- ✅ Error states with retry options
- ✅ Keyboard accessible buttons

## 🎬 Animation States

### Connection Status
```
Offline → Connecting → Online
  🔴   →      ⚠️     →   🟢
```

### Loading
```
⏳ Spinner animation (built-in)
```

### Event Log
```
New Entry:
[Top] ← Newest event
[...]
[Bottom] ← Oldest event
```

---

**Design System**: Lovable Preview Style
**Component Library**: shadcn/ui
**Icon Library**: Lucide React
**Framework**: React 18+ with TypeScript
